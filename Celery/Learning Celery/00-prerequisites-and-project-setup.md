عالی. از اینجا به بعد یک قانون برای نگارش کتاب تعیین می‌کنیم:

> **هیچ فصلی فقط تئوری نخواهد بود.**
>
> هر فصل باید در پایان یک خروجی عملی (Deliverable) داشته باشد.

بنابراین فصل ۰ را بازنویسی می‌کنیم تا واقعاً پروژه را ایجاد کنیم.

---

# فصل ۰ - آماده‌سازی محیط توسعه و ایجاد پروژه

> **هدف:** ایجاد زیرساخت پروژه‌ای که در تمام فصل‌های کتاب توسعه داده خواهد شد.

---

# اهداف یادگیری

در پایان این فصل:

* معماری کلی پروژه را خواهید شناخت.
* پروژه Django را ایجاد خواهید کرد.
* PostgreSQL و Redis را با Docker Compose اجرا خواهید کرد.
* پروژه را به Git متصل خواهید کرد.
* ساختار استاندارد پروژه را ایجاد خواهید کرد.
* برای شروع Celery آماده خواهید بود.

---

# سناریوی واقعی

فرض کنید تیم شما توسعه یک سامانه جمع‌آوری اطلاعات مالی را آغاز کرده است.

در اولین Sprint هنوز هیچ Taskای وجود ندارد.

هنوز Celery نصب نشده است.

تنها چیزی که باید انجام شود ایجاد زیرساخت صحیح پروژه است.

اشتباهی که بسیاری از پروژه‌ها انجام می‌دهند این است که مستقیماً Celery را نصب می‌کنند و بعد از چند ماه ساختار پروژه به‌هم می‌ریزد.

در این کتاب از همان ابتدا زیرساخت صحیح را ایجاد خواهیم کرد.

---

# پروژه نهایی

نام پروژه

```text
financial-data-collector
```

---

# معماری نهایی

```text
                    +----------------+
                    | Financial API  |
                    +-------+--------+
                            │
                            ▼
                      Celery Worker
                            │
                            ▼
                       PostgreSQL
                            ▲
                            │
                        Django API
                            ▲
                            │
                           Client
```

در این فصل تنها قسمت پایین شکل ایجاد خواهد شد.

---

# پیش‌نیازها

نصب بودن:

* Git
* Python 3.12+
* Docker Desktop یا Docker Engine
* Docker Compose

بررسی نسخه‌ها:

```bash
git --version
```

```bash
python --version
```

```bash
docker --version
```

```bash
docker compose version
```

---

# گام اول — ایجاد پوشه پروژه

```bash
mkdir financial-data-collector

cd financial-data-collector
```

---

# گام دوم — ایجاد Repository

```bash
git init
```

بررسی وضعیت

```bash
git status
```

---

# گام سوم — ایجاد Virtual Environment

Linux

```bash
python -m venv .venv
```

فعال‌سازی

```bash
source .venv/bin/activate
```

Windows

```powershell
.venv\Scripts\activate
```

---

# گام چهارم — نصب بسته‌های اولیه

در این فصل فقط

```bash
pip install django
```

توجه کنید:

هنوز Celery نصب نمی‌شود.

---

# گام پنجم — ایجاد پروژه Django

```bash
django-admin startproject config .
```

اجرای پروژه

```bash
python manage.py runserver
```

در مرورگر

```
http://127.0.0.1:8000/
```

اگر صفحه خوش‌آمدگویی Django نمایش داده شود، همه چیز درست است.

---

# گام ششم — ایجاد ساختار پوشه‌ها

```text
financial-data-collector/

├── config/
├── docs/
├── docker/
├── requirements/
├── .venv/
├── manage.py
├── .gitignore
└── README.md
```

دقت کنید:

فعلاً پوشه‌ای به نام

```text
celery/
```

وجود ندارد.

این پوشه در فصل سوم ایجاد خواهد شد.

---

# گام هفتم — ایجاد Docker Compose

در این فصل فقط فایل را ایجاد می‌کنیم.

فعلاً دو سرویس خواهیم داشت.

```text
docker-compose.yml
```

```text
services

PostgreSQL

Redis
```

هنوز Worker نداریم.

---

# گام هشتم — اجرای سرویس‌ها

```bash
docker compose up -d
```

بررسی

```bash
docker ps
```

باید دو Container مشاهده شود.

```text
postgres

redis
```

---

# گام نهم — اولین Commit

```bash
git add .

git commit -m "Initial project structure"
```

---

# ساختار پروژه در پایان فصل

```text
financial-data-collector/

├── config
├── docker
├── docs
├── requirements
├── docker-compose.yml
├── manage.py
├── README.md
└── .gitignore
```

---

# وضعیت پروژه

| قابلیت     | وضعیت |
| ---------- | ----- |
| Git        | ✔     |
| Django     | ✔     |
| PostgreSQL | ✔     |
| Redis      | ✔     |
| Celery     | ✖     |
| Worker     | ✖     |
| Beat       | ✖     |
| Task       | ✖     |

---

# خطاهای رایج

### Docker اجرا نمی‌شود

علت:

Docker Engine فعال نیست.

---

### صفحه Django باز نمی‌شود

علت:

Virtual Environment فعال نشده است.

---

### Redis بالا نمی‌آید

علت:

پورت 6379 توسط برنامه دیگری اشغال شده است.

---

### PostgreSQL بالا نمی‌آید

علت:

پورت 5432 قبلاً در حال استفاده است.

---

# آنچه به پروژه اضافه شد

در پایان این فصل، پروژه دارای یک زیرساخت استاندارد، قابل نسخه‌بندی و آماده برای توسعه است. از این مرحله به بعد، تمام تغییرات روی همین پایه انجام می‌شود.

---

## یک نکته مهم برای کیفیت کتاب

از فصل بعد، پیشنهاد می‌کنم **هر فصل با یک "چک‌پوینت" شروع شود**؛ یعنی ابتدا وضعیت پروژه را از انتهای فصل قبل بررسی کنیم (برای مثال با اجرای پروژه و اطمینان از سلامت سرویس‌ها)، سپس وارد توسعه قابلیت جدید شویم. این رویکرد باعث می‌شود اگر خواننده در هر مرحله دچار مشکل شده باشد، همان ابتدا آن را شناسایی و برطرف کند و کتاب به یک مسیر آموزشی قابل اعتماد و حرفه‌ای تبدیل شود.

